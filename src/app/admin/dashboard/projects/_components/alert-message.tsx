import React from 'react';

interface AppProps {
    id: string;
    title: string;
}

const AlertMessage: React.FC<AppProps> = ({id: _id, title: _title  }) => {
    return (
        <>

        </>
    );
};

export default AlertMessage;
